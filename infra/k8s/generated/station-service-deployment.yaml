apiVersion: v1
kind: Namespace
metadata:
  name: lastmile
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: station-service
  namespace: lastmile
  labels:
    app: station-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: station-service
  template:
    metadata:
      labels:
        app: station-service
    spec:
      containers:
      - name: station-service
        image: lastmile/station_service:local
        imagePullPolicy: IfNotPresent
        env:
        - name: PORT
          value: "50052"
        ports:
        - containerPort: 50052
        readinessProbe:
          tcpSocket:
            port: 50052
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 50052
          initialDelaySeconds: 20
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: station-service
  namespace: lastmile
spec:
  selector:
    app: station-service
  ports:
  - port: 50052
    targetPort: 50052
