apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: lastmile
  labels:
    app: trip-service
spec:
  type: ClusterIP
  selector:
    app: trip-service
  ports:
    - protocol: TCP
      port: 8007
      targetPort: 8007
